export { EarningsScreen } from './earnings-screen';
/** The receipt lives in `active-ride` (the completed screen shows it first); re-exported here as the earnings slice's view of it. */
export { Receipt } from '@/features/active-ride';

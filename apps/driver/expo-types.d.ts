/// <reference types="expo/types" />

// Committed twin of the generated `expo-env.d.ts` (gitignored by the Expo
// template): `process.env.EXPO_PUBLIC_*`, image module types and the typed
// router come from here in CI, where `expo start` never runs.

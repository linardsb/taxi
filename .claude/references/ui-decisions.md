# UI decisions — deferred cosmetic questions

Cosmetic questions raised mid-feature are logged here instead of debated, and resolved in the brand-identity epic.
Format: one line per question — `YYYY-MM-DD · surface · question`.

2026-08-31 · driver · «Šodien: €84.20 · Braucieni: 7» label form vs «7 braucieni» — sidesteps LV plural forms; revisit with the brand copy pass.
2026-08-31 · driver · money format «€84.20» (symbol first, dot decimal, `formatEur`) vs the Latvian «84,20 €» — the brand copy pass decides.
2026-08-31 · driver · the vehicle `category` picker — pilot is `standard` only, so the field is hidden and fixed; surface it when a second category is sold.
2026-08-31 · driver · border widths 1 px / 2 px (Button, TextField, Banner, chips) are not theme tokens — add `borderWidth` to the theme or accept the literals.
2026-08-31 · driver · focus ring: a 2 px `colors.fg` outline, offset 2 px, on every pressable (Button, language chips) — the accent-coloured border vanished on the accent-filled primary and reflowed it on focus; the brand epic picks the final ring colour and whether it becomes a theme token.
2026-09-04 · rider · `/book/status` reuses «Atjaunojam savienojumu…» for *connected but not joined to the ride room* as well as for a dropped socket — one state to the rider ("the live link is not up yet"), two causes under it; a distinct string may read better once the brand copy pass looks at the screen.
2026-09-04 · driver · the offer push reuses the `presence` Android channel (`AndroidImportance.MAX`) and the provider's hardcoded `priority: 'high'` / `sound: 'default'` — a distinct `offers` channel (own sound, per-channel mute) is a settings question for after the device day.
2026-09-04 · driver · offer-card flash = background alternating `colors.accent` / `colors.bgSurface` at 1 Hz (500 ms half-period; ≤ 3 Hz for photosensitivity), text flipping to `accentFg` on the lit half — the brand epic picks the final pair.
2026-09-04 · driver · offer tone = a 1 s looped asset: 150 ms 880 Hz sine (A5) + 850 ms silence, generated by `scripts/make-offer-tone.mjs`; pitch and cadence are placeholders until a sound pass.
2026-09-04 · driver · offer haptics = one `Warning` notification buzz on arrival, then one `Heavy` impact per second for the last 5 s of the countdown — cadence to be checked on a real phone.
2026-09-04 · driver · «Rindā: 2. no 5 · rix» renders the zone SLUG, not its name — the slug is what `driver:queue` carries; a name needs a catalog lookup or a wire change.
2026-09-09 · driver · the earnings link's accessible name is «Ieņēmumi» + the card's text joined with «. » — so the loading spinner reads «Ieņēmumi» alone and a failed first load «Ieņēmumi. —»; no separate loading copy was added (F20). The redundant `accessibilityHint` that carried «Ieņēmumi» is gone now the label opens with it. Confirm both by ear on the Level 4 device day, with F4's offer-card label.
